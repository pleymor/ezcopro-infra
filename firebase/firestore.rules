rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Default: deny all reads and writes
    // This is a placeholder - update with actual rules before deployment
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
